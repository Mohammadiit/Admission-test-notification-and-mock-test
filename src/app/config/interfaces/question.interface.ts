import {Roles} from '../enum/default.enum';
export interface question  {
  statements?: string,
  a?: string,
  b?: string,
  c?: string,
  d?: string,
  discrimination?: string,
  difficulties?: string,
  psuedoguessing?: string,
  answer?: string,
}
